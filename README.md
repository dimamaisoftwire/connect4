# Connect 4

## Setup

- Install packages: `npm install`
- Create a new database in PgAdmin4, call it `connect4`
- Copy the `.env.example` into `.env` and update the `DATABASE_URL`
- Check Prisma is connected, and run the initial migration.
